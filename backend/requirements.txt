fastapi
uvicorn
python-dotenv
# 2.15.x rejects any key that is not JWT-shaped, which breaks Supabase's current
# sb_publishable_ / sb_secret_ API keys with "Invalid API key" at startup.
supabase>=2.31.0
pytest
httpx

