import { View, Text } from 'react-native';
import screenStyles from '../styles/screenStyle';

export default function HistoryScreen() {
  return (
    <View style={screenStyles.container}>
      <Text style={screenStyles.heading}>📅 History Screen</Text>
    </View>
  );
}
